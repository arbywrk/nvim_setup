[tools]
lua = "5.4"
stylua = "latest"
lua-language-server = "latest"
ripgrep = "latest"
fd = "latest"

[tasks.format]
run = "stylua ."

[tasks.check]
run = "luac -p $(find lua -name '*.lua')"

[tasks.update]
run = "nvim --headless '+Lazy! sync' +qa"
